import { ArrowUpRight, Copy, Link2, Mail, MapPin } from 'lucide-react'
import { useState } from 'react'
import { profile } from '../data/profile'
export function Contact() { const [copied, setCopied] = useState(false); const copy = async () => { await navigator.clipboard.writeText(profile.email); setCopied(true) }; return <section id="contact" className="contact-section section-pad"><div className="contact-glow" /><div className="section-kicker">06 / open channel</div><h2>Let's build something<br /><em>secure and useful.</em></h2><p>Have a system to explore, an interface to test, or a problem worth decomposing? Send a note and let’s make the next useful thing.</p><div className="contact-actions"><a className="button button-primary" href={`mailto:${profile.email}`}><Mail size={16} /> Email me</a><button className="button button-ghost" onClick={copy}><Copy size={16} /> {copied ? 'Copied' : 'Copy email'}</button><a className="button button-ghost" href={profile.github} target="_blank" rel="noopener noreferrer"><Link2 size={16} /> GitHub <ArrowUpRight size={15} /></a><a className="button button-ghost" href={profile.linkedin} target="_blank" rel="noopener noreferrer"><Link2 size={16} /> LinkedIn <ArrowUpRight size={15} /></a></div><div className="contact-meta"><span><MapPin size={15} /> {profile.location}</span><span><Mail size={14} /> {profile.email}</span></div></section> }