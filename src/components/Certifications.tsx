import { Award, ExternalLink } from 'lucide-react'
import { certifications } from '../data/certifications'
export function Certifications() { return <section id="certifications" className="section-pad cert-section"><div className="section-heading"><div><p className="section-kicker">05 / credentials</p><h2>Signals of<br /><em>steady learning.</em></h2></div><p>Courses and simulations that have added another lens to the way I approach systems.</p></div><div className="cert-grid">{certifications.map((certification, index) => <article className="cert-card" key={certification.name}><div className="cert-icon"><Award size={18} /></div><span className="cert-number">{String(index + 1).padStart(2, '0')}</span><h3>{certification.name}</h3><p>{certification.issuer}</p><span className="cert-domain">{certification.domain}</span>{certification.url ? <a className="cert-link" href={certification.url} target="_blank" rel="noopener noreferrer">View Certificate <ExternalLink size={14} /></a> : <span className="cert-link unavailable">Certificate link unavailable</span>}</article>)}</div></section> }